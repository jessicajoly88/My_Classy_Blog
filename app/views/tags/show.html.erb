<center>
<h1><%= @tag.name %></h1>
</center>
<li>Tagged Posts:</li>
<hr>
<div class = "container">
  <ul>
  <% @tag.posts.order(created_at: :desc).each do |post| %>
    <%= link_to post.title, post_path(post) %>
  <% end %>
  </ul>
</div>

<div class="container">
<ul>
  <% @posts.each do |post| %>
    <li>
    <br>
      <div class="post-display">
        <%= link_to post.title, post_path(post) %>
        <%= link_to content_tag(:span, "", class:"glyphicon glyphicon-heart-empty"),
        edit_post_path(post.id, upvote: true) %> <small> <%= post.rating %> Likes </small>
      </div>
      <small>Posted: <%= post.created_at.time.to_formatted_s(:long) %></small>
    <br>
    </li>
  <% end %>
</ul>

<span class="glyphicon"></span>

<br>
  <% if admin_signed_in? %>
    <%=link_to "New Post", new_post_path, id: "new_post_link", class: "btn btn-info", remote: true %>
  <% end %>
<br>
<hr>

<h3> Tags </h3>
<ul>
  <% @tags.each do |tag| %>
  <br>
    <li> <%= link_to tag.name,  tag_path(tag) %> </li>
      <% if admin_signed_in? %>
        <%=link_to "Edit", edit_tag_path(tag), class:"btn btn-default" %>
        <%=link_to "Delete Tag", tag_path(tag),
                          :data => {:confirm =>"You sure?",
                          :method => "delete"}, class:"btn btn-default" %>

      <% end %>
  <% end %>
  <br>
  <br>
  <% if admin_signed_in? %>
    <%=link_to "Add Tag", new_tag_path, class:"btn btn-default" %>
  <% end %>
</ul>

</div>
